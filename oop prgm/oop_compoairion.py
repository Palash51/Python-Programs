import random
import StringIO

class WriteMyStuff(object):

    def __int__(self, write):
        self.writer = writer

    def write(self):
        write_text = "This is a silly message"
        self.write.write(write_text)


fh = open(
